<!DOCTYPE html>
<html>
  <head>
    <title></title>
  </head>

  <body>
    <% @artists.each do |a| %>
    	<a href="artists/<%=slug(a.name)%>"><%=a.name%></a>
    	<br>
    <% end %>
  </body>
</html>